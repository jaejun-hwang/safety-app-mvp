
import 'package:flutter/material.dart';

class DailyReportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('일일작업일보 입력 화면')),
      body: Center(child: Text('일일작업일보 입력 화면 내용 구현 예정')),
    );
  }
}
